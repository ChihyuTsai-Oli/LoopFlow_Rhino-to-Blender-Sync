# LoopFlow R2B 3.0 — 系統設定

本文件是維護設定、路徑與現況架構的權威來源。遷移順序見 `實作總覽.md`；契約見 `資料契約.md`。

## Repo 與版本

| 項目 | 設定 |
|---|---|
| 穩定分支 | `main`（2.x） |
| 3.0 整合分支 | `v3-development` |
| 短期工作分支 | `codex/v3-<scope>` |
| 穩定 tag | `v2.0.0`（不移動） |
| 目標版本 | `v3.0.0` |
| Rhino | 8／CPython 3.9／Windows |
| Blender（3.0 目標） | **5.2.1**（內建 Python 以該版官方為準，建環境時核對） |
| Blender addon maintainer | `Chihyu Tsai`（之後寫成的 Sync／importer 工作複本之 `bl_info`／`blender_manifest.toml`；上游 `import_3dm` 參考目錄原文不動） |

## 執行時路徑

- **設定與即時檔**：已存檔 `.3dm` 旁 `_LoopFlow_Config/loopflow_R2B/`。未存檔即停。
- 測檔環境變數：`LOOPFLOW_R2B_WORKFILES_ROOT`（雙機路徑見工作區 `工作檔路徑.md`）。**不**用 Dropbox `exchange/` 當執行時根。
- Consumer 找專案：指標檔預設＋手動覆寫（XF-ED-04＝C）。指標可在 `%APPDATA%`；語系等電腦偏好也放 AppData，不進專案設定根。
- 即時檔不提交 Git。
- 程式不寫死某台電腦的 Dropbox 絕對路徑。

## Importer

| 角色 | 路徑 | 規則 |
|---|---|---|
| 上游參考（唯讀） | `import_3dm/import_3dm-0.0.18-windows_x64/` | manifest `version = "0.0.18"`；含 wheels（如 rhino3dm 8.17.0 cp311／cp313） |
| 參考壓縮包 | `import_3dm/import_3dm-0.0.18-windows_x64.zip` | 與解壓目錄同版 |
| 實作工作複本 | `wip/` 下（建立 feature 時複製） | 只改複本；禁止改參考目錄 |
| 2.x 產品 fork | `releases/LoopFlow_import_3dm/` | 歷史對照（bl_info 曾標 0.0.50／註解 v5.0）；**非** 3.0 上游基準 |

## Repo 結構（目標）

```text
import_3dm/                    # 上游參考 0.0.18（唯讀）
releases/                      # 2.x 穩定 payload／歷史 fork
docs/USER_GUIDE*               # 公開說明
wip/
  docs/                        # 本六份正式文件＋前期規劃／＋rhino指令.md
  src/
    foundation/                # result／path／atomic／log（B02）；stub 訊息
    rhino/entrypoints/         # R2B_* 空殼入口（已落地）
    rhino/platform/            # snapshot／restore、ID 收集、Memory／Live session（B03）
    blender/loopflow_r2b_sync_dev/  # 測試 Sync add-on 空殼（獨立 package）
  tests/ fixtures/ tools/
```

## 開發測試入口

### Rhino

- 按鈕巨集：`rhino指令.md` → `wip/src/rhino/entrypoints\`（Models／Camera／Light 已接；Open 仍空殼）。
- 暫定指令：`R2B_Models`、`R2B_Camera`、`R2B_Camera_Push`、`R2B_Light`、`R2B_Light_Push`、`R2B_Open`。

### Blender（測試用 Sync add-on）

- **執行環境（雙機相同）**：Portable `E:\blender-5.2.1_wip\`（`LOOPFLOW_BLENDER_EXE`＝其下 `blender.exe`；見工作區 `工作檔路徑.md`）。
- **本機設定根**：`E:\blender-5.2.1_wip\portable\`（與日常 2.x `%APPDATA%` 隔離）。**不要**把開發原始碼主檔寫在此目錄（換機不同步）。
- **原始碼唯一來源**：`wip/src/blender/loopflow_r2b_sync_dev/`（Git）。以 junction 掛到  
  `E:\blender-5.2.1_wip\portable\scripts\addons\loopflow_r2b_sync_dev`。  
  腳本：`wip/tools/link_dev_addon.ps1`（換機 pull 後若無聯結則重跑）。
- **package ID 與 2.x 不同**，不覆蓋已發布 `LoopFlow_import_3dm`／Toolkit。
- `bl_info`：`LoopFlow R2B Sync (Dev Stub)` **0.0.5**；author `Chihyu Tsai`；Blender **5.2.1**。
- N-Panel：`LoopFlow R2B Dev`；**Models／Camera／Light 已接**；Open 仍空殼。
- **作業資料夾**：與 `.3dm`／`.blend`／`_LoopFlow_Config` **同層**；解析 `live/camera.json`、`light.json`、`models/R2B.3dm`。
- Models Update／Import 會呼叫本機已啟用的 `import_3dm.some_data`（請另啟用 R2B Pro 或 0.0.18；A05 fork 尚未進 Sync）。
- 正式／日常測法＝啟用 add-on＋面板／timer；改碼後重載 add-on（或重開 Blender）。

## 現行 2.x 安裝（唯讀參考）

```text
%APPDATA%\McNeel\Rhinoceros\8.0\scripts\LoopFlow_R2B\
  Data\   # R2B_Path.txt、Camera／Light JSON、log
  Py\     # LiveLink_R2B_*.py
```

3.0 發布時一次切換；開發期不改這套。

## 安全與驗證

- 來源文件：成功／取消／失敗／中斷後零意外變更。
- Models／Camera／Light：pending→validate→atomic；last-good 保護。
- 驗證紀錄只寫 `重構進度.md`；未驗證不得標通過。
