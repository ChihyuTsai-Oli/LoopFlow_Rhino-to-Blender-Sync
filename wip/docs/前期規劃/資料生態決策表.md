# R2B 3.0 — 資料生態決策表

這是 `現況與工作鏈藍圖.md` 的使用者編輯區。所有尚未定案、需要你確認的事項集中在本文件。回寫完成前，本文件是尚待確認事項的**唯一來源**。

## 使用方式

- 直接改「你的決定／說明」：可寫「採用」「不採用」「採用，但…」或自訂規則。
- 請保留 `XF-*`／`R2B-*` 編號，方便追蹤與回寫。
- ND 若標「由某 ED 統一裁決」，只填上方對應列。
- 你確認後，AI 回寫命名契約、系統設定、使用說明、Roadmap／PROGRESS；**確認前不寫正式產品功能碼**。

### AI 建議強度

| 強度 | 意義 |
|---|---|
| **強烈建議** | 關係資料不斷鏈、可復原、避免靜默錯誤；不採用需同等安全替代 |
| **一般建議** | 可改，但會增加規則／操作／維護成本 |
| **輕鬆建議** | 不影響核心鏈，可依習慣調整 |

---

## A. 跨產品原則（與 R2O 同文確認一次）

兩邊決策表各留一列；裁決內容與日期須相同。

| ID | 建議原則 | 現況／選項 | AI 建議（強度） | 你的決定／說明 |
|---|---|---|---|---|
| XF-ECO-01 | 工作檔未存檔：相關 producer 一律停止，不猜路徑 | A 全指令擋；B 僅 Models／寫檔／設路徑擋 | **強烈建議**：採用「未存檔即停」；先採 B（只擋會寫設定／發布檔的指令），Open 若只開說明可不擋 | 待決定 |
| XF-ECO-02 | 執行時設定與即時檔在 `.3dm` 旁 `_LoopFlow_Config/<product>/`；不用 Dropbox `exchange/` 當執行時根 | 路徑父層已定；子檔名未定 | **強烈建議**：維持已定路徑；檔名另由 ND 定 | **已採用**（2026-08-26）：父路徑；見 E 區 |
| XF-ECO-03 | 來源 Rhino 文件零變更；失敗保留 last-good | 兩邊重構計畫已寫 | **強烈建議**：採用 | **已採用** |
| XF-ECO-04 | 發布管線：暫存 → pending → validate → atomic replace last-good | 兩邊已寫 | **強烈建議**：採用 | **已採用** |
| XF-ECO-05 | Consumer 僅 parse＋apply 成功才更新「已套用」狀態 | 兩邊已寫 | **強烈建議**：採用 | **已採用** |
| XF-ECO-06 | R2B／R2O／LoopFlow／QTY 不共用 runtime package；只共用文件層安全語彙與 `_LoopFlow_Config` 父資料夾 | 已定 | **強烈建議**：採用 | **已採用** |
| XF-ECO-07 | 乾淨重建、發布時一次切換；核心不長期雙寫／alias | 已定 | **強烈建議**：採用 | **已採用** |
| XF-ED-01 | 本機偏好（介面語系等）若要做：只放 `%APPDATA%`，不寫進專案設定 | A 3.0／2.0 不做語系；B 做，AppData；C 做，寫進專案 | **一般建議**：先 A；需要再 B | 待決定 |
| XF-ED-02 | 舊版設定／scene 升級 | A 獨立 migration 必做；B 重裝／重設即可，migration 可延後 | **一般建議**：設定檔若欄位少可 B；Blender／Octane scene 內 ID 若改名再考慮 A | 待決定 |
| XF-ED-03 | 契約未定案前是否允許建立空殼 `wip/src`（bootstrap／entrypoints 佔位） | A 絕對不建；B 可建空殼與盤點並行，無業務邏輯 | **一般建議**：B，方便你先建 Rhino 按鈕（見 `rhino指令.md`） | 待決定 |
| XF-ND-01 | `_LoopFlow_Config/loopflow_R2B/` 與 `loopflow_R2O/` 內檔名是否同型（config／camera／pending／last-good／log）但允許產品專用檔 | A 同型命名規則＋產品前綴；B 完全各自命名 | **一般建議**：A，降低跨產品文件成本 | 待決定 |

---

## B. R2B 上位原則

| ID | 建議原則 | 現況／選項 | AI 建議（強度） | 你的決定／說明 |
|---|---|---|---|---|
| R2B-ECO-01 | 四邊界分開：Rhino Producer｜Blender Sync｜import_3dm fork｜Toolkit | 重構計畫已寫 | **強烈建議**：採用 | **已採用** |
| R2B-ECO-02 | 3.0 不新增同步種類或 UI 功能 | 重構計畫已寫 | **強烈建議**：採用 | **已採用** |
| R2B-ECO-03 | Toolkit 本輪不擴充、不阻塞主鏈 | 已寫 | **強烈建議**：採用 | **已採用** |
| R2B-ECO-04 | Python-first；C# 僅在有嚴格 Gate 證據時 | 已寫 | **強烈建議**：採用 | **已採用** |
| R2B-ECO-05 | 每通道（Models／Camera／Light）可單獨執行、驗證、取消、失敗復原 | 安全矩陣已列 | **強烈建議**：採用 | 待決定（確認無例外） |
| R2B-ECO-06 | Layer／物件名稱不是跨同步永久 ID；穩定 ID 另定 | 對齊 LoopFlow 精神 | **強烈建議**：採用；具體欄位見 ND | 待決定 |

---

## C. R2B 需確認（工作流／產品語意）

| ID | 需要確認 | 現況觀察／選項 | AI 建議（強度） | 你的決定／說明 |
|---|---|---|---|---|
| R2B-ED-01 | Models 選取範圍最終規則 | layer／named filter／手動選取／記住上次 `LastModelLayer` 如何並行 | **一般建議**：手動選取為主；可選「目前圖層」；記住上次當預設而非唯一真相 | 待決定 |
| R2B-ED-02 | Camera／Light 同步方式 | A 永遠自動 watcher；B 可關、可手動一次；C 僅手動 | **一般建議**：B（預設開 watcher，可關） | 待決定 |
| R2B-ED-03 | 失敗時使用者應看到什麼 | 靜默／命令列／彈窗＋stage／是否開 log 資料夾 | **一般建議**：彈窗簡述 stage＋可開 log；不自動開一堆視窗 | 待決定 |
| R2B-ED-04 | 是否要獨立「診斷／Health」指令 | A 不要，錯誤都在各指令；B 要 `R2B_Open` 內診斷；C 獨立指令 | **輕鬆建議**：先 B | 待決定 |
| R2B-ED-05 | Open 與 Config 是否分開 | A 單一 `R2B_Open`；B `Open`＋`Config` 兩指令 | **輕鬆建議**：先 A，設定項少時夠用 | 待決定 |
| R2B-ED-06 | Light 本輪同步內容 | A **只** Point 位置；B 位置＋名稱；C 再加能量／顏色 | 重構計畫傾向 A；PROGRESS golden 曾提能量／顏色。**強烈建議**：3.0 初版採 A，名稱可進 payload 但不驅動 Octane／Blender 燈參數 | 待決定 |
| R2B-ED-07 | 空選取／無燈光點／無相機 | 成功警告／阻擋／略過 | **一般建議**：Models 無選取＝阻擋；Camera／Light 無資料＝警告＋不覆寫 last-good | 待決定 |
| R2B-ED-08 | 重複同步時 Blender 端保留什麼 | 材質／自訂屬性／物件名 | **強烈建議**：明確列出「必須保留」清單進契約；預設保留使用者材質指派 | 待決定 |
| R2B-ED-09 | Auto Basic Material | A 3.0 永久排除；B 未來可開的條件 | **一般建議**：A，寫進「不屬於 3.0」 | 待決定 |
| R2B-ED-10 | 發行形態 | A Blender Extension；B 傳統 add-on ZIP；C 兩者都支援 | **一般建議**：先查目標 Blender 版本再定；暫列待補版本矩陣 | 待決定 |
| R2B-ED-11 | Toolkit 與 sync 是否同包發布 | A 同 ZIP；B 可分開裝；C 3.0 不帶 Toolkit | **一般建議**：B 或 C，避免阻塞 | 待決定 |
| R2B-ED-12 | v2→v3 使用者必須保留的升級場景 | 設定檔／Blender scene ID／collection 名／「重裝即可」 | **一般建議**：設定檔欄位對照表必做；scene 內 ID 能不改就不改 | 待決定 |

---

## D. R2B 細項（命名／檔名／schema）

| ID | 要決定什麼 | 選項 | AI 建議（強度） | 你的決定／說明 |
|---|---|---|---|---|
| R2B-ND-01 | 3.0 command ID | A 沿用 `R2B_Models`／`Camera`／`Light`／`Open`；B 改前綴或改名 | **一般建議**：A，降低學習成本；package ID 另隔離 | 待決定 |
| R2B-ND-02 | Blender package／add-on ID | A 與 2.x 不同 ID 可並裝；B 同 ID 覆蓋安裝 | **強烈建議**：A（隔離開發與穩定版） | 待決定 |
| R2B-ND-03 | 設定檔格式 | A 維持 `R2B_Path.txt`；B 改 JSON；C txt＋JSON 並存（禁止雙寫則否） | **一般建議**：B 一次切到 JSON；migration 讀舊 txt | 待決定 |
| R2B-ND-04 | Camera／Light／Models 輸出檔名 | 待你定或採建議：`camera.json`／`light.json`／`model_pending.3dm`／`model_lastgood.3dm`／`r2b.log` | **一般建議**：小寫＋角色清楚；帶 schema_version 欄 | 待決定 |
| R2B-ND-05 | 單位與座標系 | A 凍結 2.x 數學；B 重新定義並用 fixtures 鎖 | **強烈建議**：A 除非有已知錯；用 fixtures 鎖現況 | 待決定 |
| R2B-ND-06 | Collection／`_Ins`／`COL_FINAL_` 等 | A 沿用 2.x；B 重定並 migration | **一般建議**：A | 待決定 |
| R2B-ND-07 | `import_3dm` 允許的 patch 類別 | 僅列允許清單（bugfix／LoopFlow 整合掛鉤／…） | **強烈建議**：寫允許清單；禁止風格性大改 upstream | 待決定 |
| R2B-ND-08 | 設定欄去留（ModelDir、LightLayer、BoxMapSize…） | 逐欄：保留／改名／刪除 | **一般建議**：盤點後填表；LightLayer 預設是否仍 `R2B_LT_Points` 請確認 | 待決定 |
| R2B-ND-09 | 多開 Rhino／多專案鎖定 | A 不鎖；B 設定檔 exclusive lock | **一般建議**：先 A；若實機撞檔再 B | 待決定 |

---

## E. 已確認、不在本表重複決定

- 目標版本 `v3.0.0`；不移動 `v2.0.0`。
- 整合分支 `v3-development`；短期 `codex/v3-<scope>`。
- 執行時父路徑：`_LoopFlow_Config/loopflow_R2B/`（2026-08-26）。
- 測檔環境變數：`LOOPFLOW_R2B_WORKFILES_ROOT`（見工作區 `工作檔路徑.md`）。
- 來源文件零變更；last-good；pending→validate→atomic。
- 四產品邊界；不與 R2O 共用 runtime。
- 開發按鈕路徑與指令清單 SSOT：`wip/docs/rhino指令.md`。

---

## F. 回寫檢查清單（確認後由 AI 執行）

- [ ] `_R2B_命名與資料契約.md`：填 canonical 名稱與依賴表狀態
- [ ] `_R2B_系統設定.md`：檔名／入口改指向 `rhino指令.md`
- [ ] `_R2B_使用說明.md`：操作與失敗行為若改變則同步
- [ ] `architecture/DEVELOPMENT_ROADMAP.md`：A01 輸入條件
- [ ] `architecture/PROGRESS.md`：狀態與下一步
- [ ] 跨產品 XF 裁決同步到 R2O 決策表同 ID

---

## G. 下一步（使用者）

請由上而下回覆各「待決定」列，例如：`XF-ED-03 採用 B`、`R2B-ED-06 採用 A`。可一次一批（建議：先 XF → R2B-ECO → R2B-ED → R2B-ND）。
