# R2B 3.0 — 操作流程模擬（合併）

> 合併自：[`操作流程模擬_cursor_grok.md`](操作流程模擬_cursor_grok.md)、[`操作流程模擬_claude.md`](操作流程模擬_claude.md)、[`操作流程模擬_codex.md`](操作流程模擬_codex.md)。  
> 寬版閱讀：[操作流程模擬_合併.html](操作流程模擬_合併.html)。  
> **權威裁決**：[資料生態決策表_三家建議.md](資料生態決策表_三家建議.md)。  
> 主線：Models → Camera → Light。開發順序：**先完成本產品（R2B＋Blender），再 R2O**。

## 已定裁決摘要（與決策表一致）

| ID | 決定 |
|---|---|
| `R2B-ED-01` | 指定專案圖層；類別勾選彈窗；Point／Curve **預設不勾**；無視隱藏鎖定當可見匯出；排除靠勾選而非寫死；來源狀態還原 |
| `R2B-ED-02` | Camera／Light watcher 可關、可手動 |
| `XF-ED-02` | 不升級、不可混用；可有唯讀舊設定對照 |
| `R2B-ND-04` | `models/model.3dm`＋pending；`live/camera.json`、`light.json` |
| `R2B-ND-05` | Rhino 原生單位／軸向；Blender 端換算 |
| `R2B-ND-09` | 先不鎖（A） |

---

## 示範專案與磁碟

- Rhino：`Demo_Apt.3dm`（已存檔）；Blender：`Demo_Apt_R2B.blend`（隔離 3.0）
- 設定根：`_LoopFlow_Config/loopflow_R2B/`

```text
config.json / r2b.log
live/camera.json | camera_pending.json
live/light.json  | light_pending.json
models/model.3dm | model_pending.3dm
```

---

## A. 開案

1. 隔離 2.x／3.0（不同 Blender profile；不同設定根）。
2. 開啟已存檔 `.3dm`（未存檔則發布停）。
3. `R2B_Open`＋Health（路徑、三通道 last-good、Blender Sync Folder）。
4. Blender：指標或手動路徑（手動優先）。

---

## B. Models → Camera → Light

### B1. Models

5. 選**專案圖層**（可沿用上次成功）。
6. 彈出**類別勾選**：預設勾選 Mesh 等模型類；**Point、Curve 預設不勾**（可臨時勾）。顯示將匯出物件數。
7. `R2B_Models`：依物件 ID 在暫存處理；無視隱藏／鎖定／凍結當可見；禁止 `_SelAll`。
8. pending → validate → atomic `model.3dm`；來源狀態還原。
9. Blender 更新模型；保留材質等 Blender-owned。空範圍＝阻擋。

### B2. Camera

10. 透視視角；watcher 可關或手動推一次。
11. `live/camera.json` atomic；套用成功才更新狀態。

### B3. Light

12. Point 在 LightLayer；只同步位置＋ GUID。
13. 無點／圖層錯 → 不發布空清單、不清 Blender 燈。
14. 第一輪齊備。

---

## C. 第二輪

15. 改幾何 → Models → Blender 更新。
16. 改視角／燈點 → Camera／Light。

---

## D. 失敗停點（摘要）

未存檔停；Models 失敗仍在原工作檔；Light 空層不清燈；JSON 半寫不 apply。

## E. 最小可測

Camera 按鈕 → Health／log → Light → Camera atomic → Models → 拆 Blender fork。

## F. 不涵蓋

Toolkit／Auto Basic Material；自動升級；能量顏色回寫 Rhino。
