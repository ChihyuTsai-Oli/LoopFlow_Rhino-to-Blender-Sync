# LoopFlow R2B 3.0 — 重構進度

- 建立日期：2026-08-12
- 目標版本：`v3.0.0`
- 整合分支：`v3-development`（已含 Camera＋Light；`d2af81a`）
- 短期工作分支：**`codex/v3-models-vertical`**（`e773a98`；**尚未**合入整合分支）
- 穩定回復點：`v2.0.0`
- 狀態：Camera／Light **公司機已通過**並在整合分支；Models **公司機可匯出＋Import**（內嵌 import_3dm）；Update wipe／相機 debounce **已規劃未實作**；Open 仍空殼

## AI 接手入口

依序讀：`實作總覽.md` → `資料契約.md` → `工作流程.md` → `開發任務與路徑.md` → `系統設定.md` → 本文件。  
契約細節未盤完時可查 `前期規劃/資料生態決策表_三家建議.md`；測試按鈕見 `rhino指令.md`。  
開發節奏：**能做就做、需確認再停**（見 `開發任務與路徑.md` 執行規則）。

## 驗證紀錄

| 日期 | 分支 / commit | 檢查 | 結果 | 限制 |
|---|---|---|---|---|
| 2026-08-28 | `codex/v3-models-vertical` `e773a98` | **A05 起步**：import_3dm 0.0.18 **內嵌 Sync**；自解壓 rhino3dm wheels；公司機 Import **通過** | **使用者通過 Import** | Update 仍會產生 `M3D.001`（見下一步）；勿再啟用獨立 Import Rhinoceros 3D |
| 2026-08-28 | `codex/v3-models-vertical` | Models 匯出：`R2B.3dm`、排除標記可自訂、子樹圖層、父::末端材質、成功彈窗、Views 寫入 | 公司機可開檔目視 | 顯示模式偶發仍待確認；pending 鎖定已重試 |
| 2026-08-28 | `v3-development` `d2af81a` | **FF 合入** Camera D01＋Light D02 | 已 push | — |
| 2026-08-28 | `codex/v3-light-vertical` | **Light 公司機實機** | **使用者通過** | 已合入 `v3-development` |
| 2026-08-28 | `codex/v3-camera-vertical` | **Camera 公司機實機** | **使用者通過** | 已合入 `v3-development`；**自動同步 orbit 仍 lag**（待 debounce） |

（更早 Models UX／File3dm 修補紀錄見 Git 歷史 `codex/v3-models-vertical`。）

## 下一步（換機後優先）

1. **相機 lag**：`RhinoView.Modified` 每幀 atomic 寫盤 → debounce＋姿態未變略過＋熱路徑減輕（計畫已定，未寫碼）。
2. **Models Update**：對齊 2.x **wipe `R2B` 子樹再重建**＋依名稱 restore 顯隱／`hide_select`；消掉 `M3D.001`；涵蓋移／刪／換層（計畫已定，未寫碼）。
3. Models 完整驗收後再授權合入 `v3-development`。
4. Open／Health（E02）；凍結檔名需確認再停。
5. 家中：pull → `wip/tools/link_dev_addon.ps1` → 只啟用 **LoopFlow R2B Sync (Dev Stub)**。

## Golden workflow 基準

- Models：Import 公司機已通；Update wipe／材質保留／刪改層待實作後再驗。
- Camera：功能通過；自動同步順暢度待修。
- Light：公司機已驗。
- Importer：已內嵌於 Sync（A05 起步）。
- Toolkit：不阻塞主鏈。

未驗證不得標通過。
