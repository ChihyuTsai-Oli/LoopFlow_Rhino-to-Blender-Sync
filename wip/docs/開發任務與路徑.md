# LoopFlow R2B 3.0 — 開發任務與路徑

本文件把 3.0 重構拆成可在單一工作時段完成、驗證、提交與推送的單位。契約以 `資料契約.md` 為準；即時狀態只記 `重構進度.md`。

## 執行規則

- **能做就做、需確認再停**（R2B／R2O 共用開發原則）：AI 在已決契約與本檔範圍內直接推進文件、空殼、可自動驗證的實作並提交推送短期分支；僅在下列情況停下來問使用者——改已決 ED／ECO、凍結使用者可見名稱／檔名／package 顯示名、破壞性操作、本機安裝正式 runtime、或與決策明顯衝突無法用「3.0 修正」帶過。
- 一次只改一個 repo；同一時間一台電腦／一個 AI。
- 每項從 `v3-development` 建 `codex/v3-<scope>`（文件波次可暫掛既有決策分支，合入前再整理）。
- **可測＝兩端同批**：同一功能的 Rhino producer 與 Blender consumer 同一任務／同一 PR 範圍交付，不拆成「先做完一端再開另一端」。
- Rhino、Blender Sync、importer fork、Toolkit 四邊界；任務不跨邊界塞無關整理。
- 開發按鈕指向 `wip/src/rhino/entrypoints/`；正式安裝／RC 才用隔離 `%APPDATA%` 與 Blender profile。
- **Blender 測試 add-on**：B01／B04 可先交付 N-Panel 空殼按鈕（對齊 Rhino 入口），再於 C／D 垂直任務接功能；隔離 profile、獨立 package ID。
- `前期規劃/` 當原則；無明顯衝突可在實作微調，衝突先改決策表。
- Importer：從 `import_3dm/import_3dm-0.0.18-windows_x64` **複製**到 `wip/` 再改。

## 階段與任務

| ID | 任務／scope | 前置 | 主要產出 | 完成檢查 |
|---|---|---|---|---|
| R2B-A01 | 工作流盤點／`workflow-inventory` | 無 | Models／Camera／Light 兩端輸入輸出、state、副作用、失敗條件 | 只改文件；可追溯現行 2.x |
| R2B-A02 | 命名契約加厚／`naming-contract` | A01 | command、設定欄、檔名凍結表 | 使用者可見變更已寫入 `資料契約.md` |
| R2B-A03 | schema／`sync-schema` | A01–A02 | Camera／Light／Models schema＋成功條件 | 兩端共用 fixtures 規格 |
| R2B-A04 | Blender ID／`blender-contract` | A01–A03 | operator／property／panel／timer key | 與 2.x package 隔離 |
| R2B-A05 | fork 邊界／`fork-boundary` | A01、A04 | 自 0.0.18 複製之工作複本＋`UPSTREAM.md`／`PATCHES.md` | 參考目錄未改 |
| R2B-A06 | fixtures／`contract-fixtures` | A03–A05 | 可機器比對預期 | 無私人場景 |
| R2B-B01 | source 骨架／`source-skeleton` | A02–A06 | `wip/src` layout、entrypoints、tests | 可載入、不覆蓋 2.x |
| R2B-B02 | foundation／`foundation-core` | B01 | result、log、path、atomic publish | 純 Python 測試 |
| R2B-B03 | Rhino adapter／`rhino-platform` | B01–B02 | ID 收集、暫存、來源 snapshot／restore | 成敗中斷還原 |
| R2B-B04 | Blender bootstrap／`blender-bootstrap` | B01–B02、A04 | register／reload、隔離 package ID；**可含 N-Panel 空殼按鈕** | 啟停不留半套；無業務邏輯亦可先交 |
| R2B-C01 | **Models 兩端**／`models-vertical` | A03、B02–B04、A05 | Rhino 發布＋Blender 更新同批 | 來源零變更、last-good、材質保留 |
| R2B-D01 | **Camera 兩端**／`camera-vertical` | A03、B02–B04 | producer＋consumer＋開／關自動同步 | 壞檔不 apply；開關可測 |
| R2B-D02 | **Light 兩端**／`light-vertical` | A03、B02–B04 | 只位置；空層不清燈 | 與 ED-06／07 一致 |
| R2B-E01 | UI／lifecycle／`sync-ui` | C01、D01、D02 | panels、timer 生命週期 | 重載／停用實機 |
| R2B-E02 | Open／Config／`rhino-diagnostics` | B02、A02 | Health、路徑、指標檔 | 不混 Blender UI |
| R2B-F01 | importer hardening／`importer-fixtures` | C01、A05–A06 | patch＋ABI | 每 patch 可追溯 |
| R2B-F02 | Toolkit baseline／`toolkit-baseline` | 主鏈穩 | 不擴充、不阻塞 | 既有操作可選測 |
| R2B-G01 | migration／`migration` | schema 穩 | 掃描／備份／一次轉／回滾 | 只測副本 |
| R2B-G02 | build／`build-release` | 核心完成 | version、ZIP、ABI、RHC | 資產一致 |
| R2B-G03 | RC／`rc-validation` | G01–G02 | 隔離端到端紀錄 | 成敗中斷 last-good |

## 建議波次

1. A01–A06：契約、0.0.18 邊界、fixtures。
2. B01–B04：兩端可載入骨架。
3. **C01 → D01 → D02**：各通道垂直打通（每通道兩端同批）。
4. E01–E02：UI／Open。
5. F–G：fork、Toolkit、migration、RC。

## 雙機檢查

換機前：工作樹乾淨、分支已 push、與 origin `0/0`，`重構進度.md` 記下一步。私人 `.3dm`／`.blend` 不代替 Git。
