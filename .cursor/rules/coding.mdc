---
description: R2B 程式開發慣例
globs: "**/*.{py,cs}"
alwaysApply: false
---

# 程式開發慣例

所有工作先遵守工作區 `AGENTS.md`；本檔只補充 R2B 專屬原則。

## 語言選擇

- Rhino producer 與 Blender add-on 預設使用 Python。
- C# 只在 Rhino host、事件生命週期、型別或已量測效能問題有明確收益時採用。
- 不建立 C# command 只為呼叫 Python script 的多層 wrapper。

## 文件與註解

- 不得批次刪除註解。
- 保留 shebang、encoding、`bl_info`、執行 metadata、copyright 與第三方授權標頭。
- 長篇模組說明以 `docs/` 為 SSOT；程式頂部保留必要 metadata、一句用途與 `Docs:`。
- 程式內註解用於說明非顯而易見的理由、限制與安全條件，不重述程式語法。

## 硬編碼集中管理

- 使用者可調的路徑、檔名與選項由 config/schema 集中管理。
- Rhino/Blender API ID、內部屬性鍵、暫存前綴等放在所屬模組，不為了「集中」全部暴露成使用者設定。
- Models/Camera/Light 的 schema、版本與成功定義各有唯一來源。

## 安全

- 不在目前 Rhino 文件做不可回復的匯出轉換。
- 新輸出驗證前保留上一份有效檔。
- 禁止 `except: pass` 後回報成功；錯誤需指出階段與復原狀態。
